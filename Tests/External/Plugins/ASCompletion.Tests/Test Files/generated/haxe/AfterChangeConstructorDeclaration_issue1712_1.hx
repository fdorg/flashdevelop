package;
import Type.ValueType;
import haxe.Timer;
class Main {
	public function Main() {
		new Some(new Array<haxe.Timer->Type.ValueType>());
	}
}

class Some {
	public function new(array:Array<Timer->ValueType>) {
		
	}
}