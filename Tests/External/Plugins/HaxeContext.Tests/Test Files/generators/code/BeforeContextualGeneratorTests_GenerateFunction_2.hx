package;
class EFoo {
	public function new() {
		fo$(EntryPoint)o([
			[1, 2, 3, 4],
			"12345"
				.charAt(Math.random() * 5)
				.split("")
				.concat(["1", "2", "/", "\\"])
				.toString(),
			(v:String)
		], 1, 1.0, "123", {x:Int, y:Int});
	}
}