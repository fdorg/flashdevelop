package;
class Main implements IBar, IF$(EntryPoint)oo {
	public function new() {
	}
}

interface IBar {
	function bar():Void;
}

interface IFoo {
	var foo(get, set):haxe.ds.Vector<haxe.Timer->Type.ValueType>;
}