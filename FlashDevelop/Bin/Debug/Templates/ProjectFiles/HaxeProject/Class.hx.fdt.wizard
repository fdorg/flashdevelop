package $(Package);

$(Import)/**
$(CBI)* ...
$(CBI)* @author $(DefaultUser)
$(CBI)*/
$(ClassMetadata)class $(FileName)$(Extends)$(Implements)$(CSLB){

	$(Access)function new($(ConstructorArguments)) $(CSLB){
		$(Super)$(EntryPoint)
	}
	
}