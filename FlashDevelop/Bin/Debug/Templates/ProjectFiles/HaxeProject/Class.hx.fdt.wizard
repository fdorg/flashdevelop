package $(Package);

$(Import)/**
$(CBI)* ...
$(CBI)* @author $(DefaultUser)
$(CBI)*/
class $(FileName)$(Extends)$(Implements)$(CSLB){

	$(Access)function new($(ConstructorArguments)) $(CSLB){
		$(Super)$(EntryPoint)
	}
	
}